import DueComponent from '@/Components/Finance/Due/DueComponent';

const DueContainer = ({ data, filterData }) => {
    return <DueComponent data={data} filterData={filterData} />;
};

export default DueContainer;
